package hibernate.base;

import org.hibernate.Hibernate;
import org.hibernate.Session;
import hibernate.dao.ContenirDAO;

/**
 * This class has been automatically generated by Hibernate Synchronizer.
 * For more information or documentation, visit The Hibernate Synchronizer page
 * at http://www.binamics.com/hibernatesync or contact Joe Hudson at joe@binamics.com.
 *
 * This is an automatically generated DAO class which should not be edited.
 */
public abstract class BaseContenirDAO extends hibernate.dao._RootDAO {

	public static ContenirDAO instance;

	/**
	 * Return a singleton of the DAO
	 */
	public static ContenirDAO getInstance () {
		if (null == instance) instance = new ContenirDAO();
		return instance;
	}

	/**
	 * hibernate.dao._RootDAO _RootDAO.getReferenceClass()
	 */
	public Class getReferenceClass () {
		return hibernate.Contenir.class;
	}
	
	public hibernate.Contenir load(hibernate.Contenir key)
		throws org.hibernate.HibernateException {
		return (hibernate.Contenir) load(getReferenceClass(), key);
	}

	public hibernate.Contenir load(hibernate.Contenir key, Session s)
		throws org.hibernate.HibernateException {
		return (hibernate.Contenir) load(getReferenceClass(), key, s);
	}

	public hibernate.Contenir loadInitialize(hibernate.Contenir key, Session s) 
			throws org.hibernate.HibernateException { 
		hibernate.Contenir obj = load(key, s); 
		if (!Hibernate.isInitialized(obj)) {
			Hibernate.initialize(obj);
		} 
		return obj; 
	}

	/**
	 * Persist the given transient instance, first assigning a generated identifier. (Or using the current value
	 * of the identifier property if the assigned generator is used.) 
	 * @param contenir a transient instance of a persistent class 
	 * @return the class identifier
	 */
	public hibernate.Contenir save(hibernate.Contenir contenir)
		throws org.hibernate.HibernateException {
		return (hibernate.Contenir) super.save(contenir);
	}

	/**
	 * Persist the given transient instance, first assigning a generated identifier. (Or using the current value
	 * of the identifier property if the assigned generator is used.) 
	 * Use the Session given.
	 * @param contenir a transient instance of a persistent class
	 * @param s the Session
	 * @return the class identifier
	 */
	public hibernate.Contenir save(hibernate.Contenir contenir, Session s)
		throws org.hibernate.HibernateException {
		return (hibernate.Contenir) super.save(contenir, s);
	}

	/**
	 * Either save() or update() the given instance, depending upon the value of its identifier property. By default
	 * the instance is always saved. This behaviour may be adjusted by specifying an unsaved-value attribute of the
	 * identifier property mapping. 
	 * @param contenir a transient instance containing new or updated state 
	 */
	public void saveOrUpdate(hibernate.Contenir contenir)
		throws org.hibernate.HibernateException {
		super.saveOrUpdate(contenir);
	}

	/**
	 * Either save() or update() the given instance, depending upon the value of its identifier property. By default the
	 * instance is always saved. This behaviour may be adjusted by specifying an unsaved-value attribute of the identifier
	 * property mapping. 
	 * Use the Session given.
	 * @param contenir a transient instance containing new or updated state.
	 * @param s the Session.
	 */
	public void saveOrUpdate(hibernate.Contenir contenir, Session s)
		throws org.hibernate.HibernateException {
		super.saveOrUpdate(contenir, s);
	}

	/**
	 * Update the persistent state associated with the given identifier. An exception is thrown if there is a persistent
	 * instance with the same identifier in the current session.
	 * @param contenir a transient instance containing updated state
	 */
	public void update(hibernate.Contenir contenir) 
		throws org.hibernate.HibernateException {
		super.update(contenir);
	}

	/**
	 * Update the persistent state associated with the given identifier. An exception is thrown if there is a persistent
	 * instance with the same identifier in the current session.
	 * Use the Session given.
	 * @param contenir a transient instance containing updated state
	 * @param the Session
	 */
	public void update(hibernate.Contenir contenir, Session s)
		throws org.hibernate.HibernateException {
		super.update(contenir, s);
	}

	/**
	 * Remove a persistent instance from the datastore. The argument may be an instance associated with the receiving
	 * Session or a transient instance with an identifier associated with existing persistent state. 
	 * @param contenir the instance to be removed
	 */
	public void delete(hibernate.Contenir contenir)
		throws org.hibernate.HibernateException {
		super.delete(contenir);
	}

	/**
	 * Remove a persistent instance from the datastore. The argument may be an instance associated with the receiving
	 * Session or a transient instance with an identifier associated with existing persistent state. 
	 * Use the Session given.
	 * @param contenir the instance to be removed
	 * @param s the Session
	 */
	public void delete(hibernate.Contenir contenir, Session s)
		throws org.hibernate.HibernateException {
		super.delete(contenir, s);
	}
	
	/**
	 * Re-read the state of the given instance from the underlying database. It is inadvisable to use this to implement
	 * long-running sessions that span many business tasks. This method is, however, useful in certain special circumstances.
	 * For example 
	 * <ul> 
	 * <li>where a database trigger alters the object state upon insert or update</li>
	 * <li>after executing direct SQL (eg. a mass update) in the same session</li>
	 * <li>after inserting a Blob or Clob</li>
	 * </ul>
	 */
	public void refresh (hibernate.Contenir contenir, Session s)
		throws org.hibernate.HibernateException {
		super.refresh(contenir, s);
	}

    public String getDefaultOrderProperty () {
		return null;
    }

}